# Friday 2.0 - Google Calendar Configuration
# Story 7.2: Sync Google Calendar Bidirectionnelle
# Configuration Calendrier Unique (centralise tous les événements)

google_calendar:
  # Enable/disable Google Calendar synchronization
  enabled: true

  # Sync interval in minutes (30 min = 48 requests/day)
  sync_interval_minutes: 30

  # Calendrier unique centralisant tous les événements (médical, enseignement, recherche, personnel)
  calendars:
    - id: "primary"  # Calendrier principal Google (pas besoin de le remplacer)
      name: "Calendrier Friday"
      casquette: "personnel"  # Casquette par défaut (peut être changée dynamiquement par Friday)
      color: "#4285F4"  # Bleu Google Calendar

  # Sync time range - OPTIONNEL (si absent ou null, récupère TOUT l'historique sans limite)
  # Selon doc officielle Google Calendar API: timeMin/timeMax sont optionnels
  # Si non spécifiés, l'API retourne tous les événements disponibles (Google Calendar existe depuis 2006)
  sync_range: null  # null = pas de limite (récupère tout l'historique Google Calendar depuis sa création)

  # Alternative: spécifier des limites explicites si besoin
  # sync_range:
  #   past_days: 7300    # ~20 ans historique (jusqu'à 2006, lancement Google Calendar)
  #   future_days: 18250  # 50 ans futur (planification jusqu'en 2076)

  # Default reminders for new events
  default_reminders:
    - method: "popup"
      minutes: 30      # Notification 30 min avant
